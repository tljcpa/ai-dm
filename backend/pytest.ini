[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
# 跳过 requires_api 标记的测试（CI 无 API key 时自动 skipif 也起作用）
markers =
    requires_api: 需要真实 LLM/embedding API key 才能跑（CI 默认跳过）
